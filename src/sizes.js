import * as sizes from "./constants";

// button
export default `
  .cssinjs-button-lg {
    padding: ${sizes.paddingY} ${sizes.paddingX};
    font-size: ${sizes.baseFontSize};
    border-radius: ${sizes.borderRadius};
  }
`;
